from random import randint

def random_number():
    random_number = randint(1, 10)

    #End the statement and return back the random number 

number = #Fill in the end of this statement by calling the random_number function

user_input = input("Enter a number between 1-10")

user_input = int(user_input)

while user_input != number:
    if user_input == number:
        #Tell the user that they won
        break
    else:
        #Tell the user to go again using the user_input variable
        
